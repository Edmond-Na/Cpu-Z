<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.soc.SocFragment">

        <ImageView
            android:id="@+id/process_brand_imageView"
            android:layout_width="75dp"
            android:layout_height="75dp"
            android:layout_margin="12dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="8dp"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="@+id/process_brand_imageView"
            app:layout_constraintStart_toEndOf="@+id/process_brand_imageView"
            app:layout_constraintTop_toTopOf="@+id/process_brand_imageView">

            <TextView
                android:id="@+id/process_name_textView"
                style="@style/MainText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Process Name" />

            <TextView
                android:id="@+id/process_speed_textView"
                style="@style/MainText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Process Speed"/>

        </LinearLayout>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
